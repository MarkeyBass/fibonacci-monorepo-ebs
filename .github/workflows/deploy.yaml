name: Deploy MultiDocker
on:
  push:
    branches:
      - main # check your repo, your default branch might be master!

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t markeybass/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true markeybass/react-test npm test

      - run: docker build -t markeybass/multi-client ./client
      - run: docker build -t markeybass/multi-nginx ./nginx
      - run: docker build -t markeybass/multi-server ./server
      - run: docker build -t markeybass/multi-worker ./worker

      - run: docker push markeybass/multi-client
      - run: docker push markeybass/multi-nginx
      - run: docker push markeybass/multi-server
      - run: docker push markeybass/multi-worker
